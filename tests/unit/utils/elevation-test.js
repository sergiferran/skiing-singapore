import elevation from '../../../utils/elevation';
import { module, test } from 'qunit';

module('Unit | Utility | elevation');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = elevation();
  assert.ok(result);
});
