import { baseZero } from '../../../helpers/base-zero';
import { module, test } from 'qunit';

module('Unit | Helper | base zero');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = baseZero(42);
  assert.ok(result);
});
